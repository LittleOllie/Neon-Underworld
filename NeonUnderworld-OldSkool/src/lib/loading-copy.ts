/** Route-level loading labels — shown when route content suspends. */
export { routeSuspenseLoadingMessage as routeLoadingMessage } from '@local/config/navigation-transition';

export { ACTION_PENDING } from './loading-copy-actions';
